# ─── SmartFarm DakLak - Smart Control Configuration ───
# Generate a secure JWT secret first:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Copy to .env and fill in your values. Never commit .env to git.

# Server
PORT=3002

# MQTT
MQTT_URL=mqtt://localhost:1883

# JWT Authentication (REQUIRED — generate a secure random value)
JWT_SECRET=REPLACE_ME

# User passwords (REQUIRED — generate secure values)
ADMIN_PASSWORD=REPLACE_ME
OPERATOR_PASSWORD=REPLACE_ME
VIEWER_PASSWORD=REPLACE_ME

# InfluxDB (optional — service runs with in-memory fallback if not configured)
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=
INFLUXDB_ORG=smartfarm
INFLUXDB_BUCKET=soil_data

# Simulator (for development)
SIMULATOR_URL=http://localhost:3001
